every :{{ cron_period_check }} do # Many shortcuts available: :hour, :day, :month, :year, :reboot
  command "source /home/{{ username }}/.rvm/scripts/rvm && ruby /home/{{ username }}/.lib/scripts/ssl_check.rb"
end
